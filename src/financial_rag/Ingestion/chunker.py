"""文本分块。

状态：架构占位，尚未实现业务逻辑。

职责与输入输出：
输入：清洗后的页面与分块配置。输出：Chunk 列表。
优先沿章节和段落切分，过长内容按配置的 token 预算切分并添加 overlap。
生成确定性的 chunk_id；保留来源页码、标题和章节。

边界与约束：
不生成 embedding。明确 tokenizer 与计数口径；没有结构信息时允许退化为段落分块。
"""
